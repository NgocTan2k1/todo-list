export type Member = {
    userId: string;
    username: string;
};

export type TaskRefer = {
    taskId: string | '';
    taskName: string | '';
};
export type ActualWorkingTime = {
    actualDate: Date;
    actualtime: number;
};
